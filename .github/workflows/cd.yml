name: CD

on: 
    workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Log in to the Container registry
      uses: docker/login-action@v3
      with:
        registry: https://ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build and Push Docker Image
      uses: docker/build-push-action@v6
      with:
        context: .
        push: true
        tags: |
          ghcr.io/${{ github.repository_owner }}/web:latest
          ghcr.io/${{ github.repository_owner }}/web:${{ github.sha }}

    # Step 4: Upload docker-compose.yml to remote server
    # - name: Upload Docker Compose File to Remote Server
    #   uses: appleboy/scp-action@v0.2.3
    #   with:
    #     host: ${{ secrets.REMOTE_HOST }}
    #     username: ${{ secrets.REMOTE_USER }}
    #     key: ${{ secrets.SSH_PRIVATE_KEY }}
    #     source: ./docker-compose.yml
    #     target: /home/${{ secrets.REMOTE_USER }}/docker-compose.yml

    # # Step 5: SSH into remote server and run Docker Compose
    # - name: Deploy App on Remote Server
    #   uses: appleboy/ssh-action@v0.1.10
    #   with:
    #     host: ${{ secrets.REMOTE_HOST }}
    #     username: ${{ secrets.REMOTE_USER }}
    #     key: ${{ secrets.SSH_PRIVATE_KEY }}
    #     script: |
    #       cd /home/${{ secrets.REMOTE_USER }}
    #       docker compose pull
    #       docker compose up -d
